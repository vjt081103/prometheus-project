groups:
  - name: 'CPU up'
    rules:
    - record: cpu_usage_percent
      expr: abs(node_cpu_seconds_total - 1.1 * holt_winters(node_cpu_seconds_total[5m],0.2,0.3))
    - alert: High CPU
      expr: cpu_usage_percent > 0
      for: 9s
      labels:
        severity: high